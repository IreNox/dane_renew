{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "type": "shell",
	"echoCommand": true,
    "cwd": "${workspaceFolder}",
    "tasks": [
        {
            "label": "Build",
            "command": "go install github.com/IreNox/letsencrypt_helper",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}